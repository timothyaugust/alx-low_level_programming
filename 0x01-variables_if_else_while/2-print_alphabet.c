#include <stdlib.h>

/**
 * main - main function
 *
 * Return: Always 0
 */

int main(void)
{
        char i;
        for (i = 'a'; c <= 'z'; i++)
          putchar(i);
        putchar('\n');
        return (0);
}
